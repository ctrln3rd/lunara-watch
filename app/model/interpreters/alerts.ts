import { Weather, Intent } from '../../types';
export default function alertsInterpreter(weather: Weather, intent: Intent): string {
// TODO: Summarize severe weather alerts or warnings
return `The alerts intent was detected.`;
}