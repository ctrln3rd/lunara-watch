import { Weather, Intent } from '../../types';
export default function uvInterpreter(weather: Weather, intent: Intent): string {
// TODO: Interpret UV index risk and protection advice
return `The UV intent was detected.`;
}