import { Weather, Intent } from '../../types';
export default function humidityInterpreter(weather: Weather, intent: Intent): string {
// TODO: Provide humidity averages or comfort level insights
return `The humidity intent was detected.`;
}