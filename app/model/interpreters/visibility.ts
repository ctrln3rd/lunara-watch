import { Weather, Intent } from '../../types';
export default function visibilityInterpreter(weather: Weather, intent: Intent): string {
// TODO: Describe visibility range (good, moderate, poor)
return `The visibility intent was detected.`;
}