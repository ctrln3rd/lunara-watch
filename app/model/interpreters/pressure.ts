import { Weather, Intent } from '../../types';
export default function pressureInterpreter(weather: Weather, intent: Intent): string {
// TODO: Summarize barometric trends or pressure forecasts
return `The pressure intent was detected.`;
}